# Request
sensor_msgs/NavSatFix[] waypoints    # Array of waypoints (lat, lon, alt)
float64 hold_time                    # Hold time at each waypoint (seconds)
float64 acceptance_radius            # Acceptance radius for each waypoint (meters)
float64[] yaws                       # Optional yaw angles in degrees (empty array if not used)

---

# Response
bool success                         # True if waypoints were successfully pushed
string message                       # Status message
uint32 waypoints_pushed             # Number of waypoints actually pushed
